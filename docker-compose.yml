services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: recall
      POSTGRES_PASSWORD: recall
      POSTGRES_DB: recall
    ports:
      - "5432:5432"
    volumes:
      - recall_pgdata:/var/lib/postgresql/data
volumes:
  recall_pgdata: